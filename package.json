{
  "name": "Ping",
  "version": "1.0.0",
  "description": "Node based chat server",
  "main": "server.js",
  "scripts": {
    "start": "echo $NODE_ENV; if [ $NODE_ENV == 'production' ]; then node server.js; else nodemon server.js; fi",
    "postinstall": "bower cache clean && bower install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "chat"
  ],
  "author": "SK Manoj",
  "license": "ISC",
  "dependencies": {
    "bower": "^1.4.1",
    "express": "^4.12.3",
    "gravatar": "^1.1.0",
    "morgan": "^1.5.2",
    "nodemon": "^1.3.7",
    "socket.io": "^1.3.5"
  },
  "engines": {
    "node": "0.12.x"
  }
}
